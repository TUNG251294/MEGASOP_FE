export const productGroupsCount = (state) => {
    return state.groups ? state.groups.length : 0;
}

export const levelGroupsCount = (state) => {
    return state.levelGroups ? state.levelGroups.length : 0;
}